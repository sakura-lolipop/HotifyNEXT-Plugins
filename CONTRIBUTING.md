# 贡献插件

## 提交方式

PR 到 `plugins/`：**一个插件一个 yaml 文件**，文件名=服务名（小写连字符）。写法照 [hooks.md](hooks.md)（字段表/验签四族/翻译指引 §10）——最省事的路径是把 hooks.md + 目标服务的源码/文档地址交给任意 AI 助手代写，你检查产出。

**提交 PR 即按 [MIT](LICENSE) 授权本仓收录与分发**（inbound=outbound；你保留自己插件的版权）。

## 审核清单（维护者用，也供贡献者自查）

插件是纯数据（引擎白名单保证无代码执行），审核只看目录卫生，一分钟一个 PR：

1. `id` 与现有插件不冲突（`^[a-z0-9][a-z0-9-]{1,31}$`）
2. `device` 为空（广播）或合法 slug/uuid——不含主密钥、不含可疑指向
3. `mapping` 引用的字段与服务真实 payload 匹配（examples 源自真实捕获/源码，非凭空想象——hooks.md §5 铁律）

可选自测（想跑就跑）：把 yaml 放进你自托管 Hotify 的 `hooks/` 目录 + 设对应 `HOOKS_<ID>_SECRET` 环境变量 + 重启——启动自检不过会直接 fatal 指出哪里错（`[hooks] loaded` 行确认装上）。

## 服务名与商标

插件文件使用服务名（Grafana/Sonarr/…）属描述性引用；不要使用各服务官方 logo/素材。
